/**
 * Client Supabase — placeholder.
 *
 * Quand le backend sera prêt :
 *   npx expo install @supabase/supabase-js
 *
 *   import { createClient } from '@supabase/supabase-js';
 *   export const supabase = createClient(
 *     process.env.EXPO_PUBLIC_SUPABASE_URL!,
 *     process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!,
 *   );
 *
 * Le schéma SQL complet est dans /supabase/schema.sql.
 * Les écrans ne parlent JAMAIS à Supabase directement : ils passent par
 * src/services/api.ts, qui basculera du mock vers ces requêtes.
 */

export const SUPABASE_READY = false;
